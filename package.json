{
  "name": "spanned-drive-frontend-rs",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build-dev": "vite build && rm -rf /var/www/sdrive/* && cp /home/avjef/spanned-drive-frontend-rs/build/* /var/www/sdrive/ -r && echo '\ncopied build to nginx folder'",
    "build-prd": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "tauri": "tauri"
  },
  "license": "MIT",
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-opener": "^2.5.3"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.55.0",
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "svelte": "^5.55.1",
    "svelte-check": "^4.4.6",
    "typescript": "~5.6.3",
    "vite": "^6.4.1",
    "@tauri-apps/cli": "^2.10.1"
  }
}
